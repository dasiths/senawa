{
  "name": "@senawa/portal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "test": "vitest run --root ../.. packages/portal",
    "typecheck": "tsc -b --pretty false"
  },
  "dependencies": {
    "@senawa/protocol": "workspace:*"
  },
  "devDependencies": {
    "vite": "8.2.1"
  }
}
